[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers --tb=short
markers =
    integration: integration tests requiring external services
    slow: slow tests (training, backtesting)
